import {fetchAllFilters,selectedFilter,selectedColor,foldFilter,showFilter,} from "./sideBarAction";
import {fetchReviewAll} from "./reviewAction";

export default {
    fetchAllFilters,
    selectedFilter,
    foldFilter,
    showFilter,
    selectedColor,
    fetchReviewAll
}